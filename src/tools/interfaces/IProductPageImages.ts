import type IProduct from "./dtos/IProduct";

/** Defines the structure of the Product Page Images component props. */
export default interface IProductPageImages {
    /** Product to view images for. */
    product: IProduct
}