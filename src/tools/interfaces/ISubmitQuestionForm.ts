/** Describes the structure for the submit FAQ question form. */
export default interface ISubmitQuestionForm {
    /** Question submitted by user. */
    question: string,
    /** User contact email. */
    email: string
}