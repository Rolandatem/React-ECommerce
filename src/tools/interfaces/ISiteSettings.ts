/** Various site-wide settings. */
export default interface ISiteSettings {
    /** Indicates whether the user is in a mobile sized page. */
    isMobile: boolean,
    /** URL to the API used in the application. */
    webAPIUrl: string
}