import type IComponentClass from "./IComponentClass";

/** Defines the structure for the section label component. */
export default interface ISectionLabel extends IComponentClass {
    /** Label to display. */
    label: string
}